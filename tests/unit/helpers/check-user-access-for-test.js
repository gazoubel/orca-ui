
import { checkUserAccessFor } from 'orca-ui/helpers/check-user-access-for';
import { module, test } from 'qunit';

module('Unit | Helper | check user access for');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = checkUserAccessFor([42]);
  assert.ok(result);
});

