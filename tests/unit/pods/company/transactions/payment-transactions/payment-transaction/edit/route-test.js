import { moduleFor, test } from 'ember-qunit';

moduleFor('route:company/transactions/payment-transactions/payment-transaction/edit', 'Unit | Route | company/transactions/payment transactions/payment transaction/edit', {
  // Specify the other units that are required for this test.
  // needs: ['controller:foo']
});

test('it exists', function(assert) {
  let route = this.subject();
  assert.ok(route);
});
