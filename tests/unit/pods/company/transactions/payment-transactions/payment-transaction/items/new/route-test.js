import { moduleFor, test } from 'ember-qunit';

moduleFor('route:company/transactions/labor-transactions/labor-transaction/items/new', 'Unit | Route | company/transactions/payment transactions/payment transaction/items/new', {
  // Specify the other units that are required for this test.
  // needs: ['controller:foo']
});

test('it exists', function(assert) {
  let route = this.subject();
  assert.ok(route);
});
