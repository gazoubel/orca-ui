import { moduleFor, test } from 'ember-qunit';

moduleFor('route:company/transactions/purchase-bills/purchase-bill/products/product/edit', 'Unit | Route | company/transactions/purchase transactions/purchase transaction/products/product/edit', {
  // Specify the other units that are required for this test.
  // needs: ['controller:foo']
});

test('it exists', function(assert) {
  let route = this.subject();
  assert.ok(route);
});
