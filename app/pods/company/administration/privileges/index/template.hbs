<!-- <button type="button" class="btn btn-default btn-circle"><i class="glyphicon glyphicon-plus"></i></button> -->
<h2>{{t 'product.administration.menu.privileges'}}<small></small></h2>
{{#link-to 'company.administration.privileges.new'}}
<button class="btn btn-default">
  <i class="glyphicon glyphicon-plus"></i>
  {{t 'product.commonWords.add'}}
</button>
{{/link-to}}

<div class="box" style="margin-top: 5px">
  <div class="box-body no-padding">
    <table class="table table-hover">
      <thead>
        <tr>
          <th>{{t 'fields.name'}}</th>
        </tr>
      </thead>
      <tbody>
        {{#each privileges as |privilege|}}
        {{#link-to 'company.administration.privileges.privilege.edit' privilege.id tagName="tr"}}
          <td>{{privilege.name}}</td>
        {{/link-to}}
        {{/each}}
    </tbody>
  </table>
  </div>
</div>
