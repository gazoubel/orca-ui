<!-- <button type="button" class="btn btn-default btn-circle"><i class="glyphicon glyphicon-plus"></i></button> -->
<h2>{{t 'product.administration.menu.itemTypes'}}<small></small></h2>
{{#show-on-click  modelIsInValid=modelIsInValid
    onCloseClicked="closeAddPanel" onAddClicked="add"
    itemAdded=newItemType }}
  <!-- <div class="form-group {{if modelIsInValid "has-error"}}">
    {{#orca-dropdown id="itemBasicType" options=basicTypes
        class="form-control"
        selected=newItemType.basicType
        placeholder = (t 'fields.basicType')
        onchange=(action (mut newItemType.basicType)) as |type|}}
      {{t type.name}}
    {{/orca-dropdown}}
  </div> -->
  <div class="form-group {{if modelIsInValid "has-error"}}">
    {{input value=newItemType.name class="form-control" id="nameInput"
      placeholder = (t 'fields.name') }}
  </div>
  <div class = "checkbox">
    <label>
      {{input checked=newItemType.isLabor id="isLaborInput" type="checkbox"}}
      Is Labor
    </label>
  </div>
{{/show-on-click}}

<div class="box" style="margin-top: 5px">
  <div class="box-body no-padding">
    <table class="table table-condensed">
      <thead>
        <tr>
          <th>{{t 'fields.name'}}</th>
          <th>{{t 'fields.basicType'}}</th>
        </tr>
      </thead>
      <tbody>
        {{#each model as |itemType|}}
        <tr>
          <td>{{itemType.name}}</td>
          <td>{{t itemType.basicTypeName}}</td>
          <!-- <td><button type="submit" class="btn btn-default" {{action "removeStage" stage}}>{{t 'product.basic.buttonText.delete'}}</button></td> -->
        </tr>
        {{/each}}
    </tbody>
  </table>
  </div>
</div>
