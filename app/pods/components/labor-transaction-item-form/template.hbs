<form  class="form-horizotal" role="form" {{action "save" laborTransactionItem on="submit"}}> <!-- horizontal form -->
  <div class="row">
    {{#orca-typeahead-with-label
      classNames="col-sm-12"
      validation=laborTransactionItem.validations.attrs.laborItem
      selected=laborTransactionItem.laborItem
      search=(action "search")
      onchange=(action (mut laborTransactionItem.laborItem))
      placeholder = (t 'product.transactions.labor-transactions.labor-transaction.labor-item')
      displayText=(t 'product.transactions.labor-transactions.labor-transaction.labor-item')
      oncreate=(action 'addNewItem')
      as |item|}}
       {{item.name}}
     {{/orca-typeahead-with-label}}
   </div>
   <div class="row">
     {{orca-input-with-label field = laborTransactionItem.description displayText=(t 'fields.description')
        classNames="col-sm-12" validation=laborTransactionItem.validations.attrs.description}}
   </div>
   <div class="row">
     {{#orca-dropdown-label
      classNames="col-sm-12"
      options=allProjects
      selected=selectedProject
      onchange=(action (mut selectedProject))
      placeholder = (t 'product.transactions.transaction.project')
      displayText=(t 'product.transactions.transaction.project')
      as |project|}}
        {{project.name}}
      {{/orca-dropdown-label}}
    </div>
    <div class="row">
      {{#orca-dropdown-label
         classNames="col-sm-12"
         validation=laborTransactionItem.validations.attrs.projectStage
         options=selectedProject.projectStages
         selected=laborTransactionItem.projectStage
         onchange=(action (mut laborTransactionItem.projectStage))
         placeholder = (t 'product.transactions.transaction.project_stage')
         displayText=(t 'product.transactions.transaction.project_stage')
         as |stage|}}
          {{stage.name}}
      {{/orca-dropdown-label}}
    </div>
    <div class="row">
      {{orca-input-with-label field = laborTransactionItem.quantity displayText=(t 'product.commonWords.quantity')
        classNames="col-sm-12" validation=laborTransactionItem.validations.attrs.quantity numeric="true" scale="2"}}
    </div>
    <div class="row">
      {{orca-input-currency-with-label field = laborTransactionItem.total displayText=(t 'product.commonWords.total')
        classNames="col-sm-12" validation=laborTransactionItem.validations.attrs.total numeric="true" scale="2"}}
    </div>
  <div class="box-footer">
    <button type="submit" class="btn btn-default pull-right">{{t 'fields.save'}}</button>
    <button class="btn btn-default pull-right" style="margin-right: 5px;" {{action "cancel" laborTransactionItem}}>{{t 'fields.cancel'}}</button>
  </div>
</form>
